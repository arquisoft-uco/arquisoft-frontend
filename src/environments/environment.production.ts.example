// Copia este archivo como environment.production.ts y completa los valores reales.
// NUNCA hagas commit de environment.production.ts con datos reales.
export const environment = {
  production: true,
  apiUrl: '/api',
  keycloak: {
    url: 'https://your-keycloak-host/',
    realm: 'your-realm',
    clientId: 'your-client-id',
  },
};
